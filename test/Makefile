# This Makefile was generated by the made script:
# https://github.com/alx741/made
#
# Dependencies:
# 	pandoc
# 	pandoc-citeproc

INPUTS = $(shell find . -type f -name '*.md')
OUTPUTS = $(foreach input, $(INPUTS), $(input:.md=.html))

all: $(OUTPUTS)

%.html: %.md
	pandoc $< -o $@ --template ../template/template.html --standalone --mathjax --toc --toc-depth 2

clean:
	rm $(OUTPUTS)
